import AuthForm from "@/components/cards/LoginForm";
import React from "react";

const page = () => {
  return (
    <div className="w-full h-screen overflow-hidden flex items-center justify-center">
      <AuthForm />
    </div>
  );
};

export default page;
